# Zylo Phase 5 — UI polish, a video grid that fits the room, chat toast, "Mute for me"

## Context

Phase 4 is merged (`origin/main` @ `e202a92`). The user's new Phase 5 is a UI phase. It replaces the old "hardening/deploy" Phase 5, which moves to Phase 7. Phase 6 (Translator Convo) is planned separately, after this one.

The main complaint: **tiles don't adjust to how many people are in the meeting.** Today [video-stage.tsx](web/components/video-stage.tsx) uses a fixed `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` grid of 16:9 tiles inside a stage that can scroll:
- One person on a laptop gets a tile a third of the width.
- Two people on a phone get two thin strips.
- Twenty people scroll.

The room is also `min-h-dvh`, so the page grows and the control bar gets pushed off-screen. Below that:
- The control bar has 7 controls at 48px each (host), which wrap to 2–3 rows at 320px.
- ZyloLive shows on phones, where screen sharing can't work.

Also asked for:
- A toast when a ZyloChat message arrives.
- A local-only "Mute for me" in the People list.
- A design pass using **ui-ux-pro-max**, built with **ponytail** laziness.

Outcome: the room fills the viewport at every width from 320px to 1440px+, with tiles resized to the number of members, and nothing scrolling sideways.

**Design source of truth:** [design-system/zylo/MASTER.md](design-system/zylo/MASTER.md) and `pages/meeting-room.md`. They were written in Phase 1, and Phase 5 does not regenerate them. The rules that apply here:
- 44px minimum touch targets with 8px spacing.
- 150–300ms CSS transitions only.
- `prefers-reduced-motion` respected.
- Visible focus states.
- No horizontal scroll.
- `dvh`, not `vh`.
- Lucide icons.
- Toasts auto-dismiss in 3–5s.

**No server changes in Phase 5.** Everything is in `web/`.

## Step 0 — Branch and docs
- `git worktree add .worktrees/phase-5-ui -b phase-5-ui origin/main`. Local `main` is stale at `7bd4713`, so branch from `origin/main`.
- In [docs/superpowers/specs/2026-09-14-zylo-design.md](docs/superpowers/specs/2026-09-14-zylo-design.md) §Phases, add Phase 5 (UI/UX + responsiveness) and Phase 6 (Zylo Translator Convo). Renumber the old phases: Hardening/deploy → 7, AI in ZyloChat → 8, Stuck detection → 9. Also fix the in-text references (line 32 "Phase 6" → 8, line 57 and line 246 "Phase 5" → 7, line 119 "Phase 6" → 8).
- Copy this plan to `docs/superpowers/plans/2026-09-23-zylo-phase-5.md`.
- Execution follows the saved workflow: Sonnet subagents, one per task below, with a review after each.

## Task 1 — Baseline audit, before touching code
Run the dev server with `preview_start`. At **320, 375, 480, 768, 1024, 1025 and 1440** pixels wide, capture the dashboard, the pre-join page (`/m/[code]`) and the room (as the only person). At each width, run:
```js
[...document.querySelectorAll('*')].filter(e => e.getBoundingClientRect().right > innerWidth + 1).map(e => e.className)
```
The findings list goes into the plan doc, and **only those findings get fixed** in Task 6. Dashboard and pre-join were already built responsive in Phase 1 (`md:grid-cols-3`, `sm:flex-row`, `lg:grid-cols-[1.4fr_1fr]`), so they get no rewrite unless the sweep finds a problem.

### Task 1 findings (2026-09-23, browser pane)
The dashboard, pre-join and room were checked at 320, 375, 480, 768, 1024, 1025 and 1440px. No page scrolls sideways at any width. The room (with Tasks 2 and 3 in) never scrolls vertically, and its control bar sits at the bottom of the screen.
- **F1** Pre-join at 320px: the size-24 avatar overlaps the mic and camera toggles in the 16:9 preview. Fixed with `size-16 sm:size-24`.
- **F2** Room at 320px on a browser that has `getDisplayMedia` (a narrow desktop window): 6 controls need 304px against 296px available, so the bar wraps to 2 rows. Fixed with `px-2` below `sm`, which gives exactly 304px. Real phones hide ZyloLive, so they get 5 controls.
- **F3** Buttons, tabs and radio options all show `cursor: default` (Tailwind v4). Fixed in globals.css.

## Task 2 — Video grid that fits the member count (the core of the phase)
**New** `web/lib/stage-layout.ts`: a pure module with no local imports, like `speaker-order.ts`.
```ts
export function fitGrid(count, width, height, gap): { cols; tileWidth; tileHeight; scroll }
```
- Try every column count `cols = 1..count`, with `rows = ceil(count/cols)`.
- Cell size is `(width-(cols-1)·gap)/cols × (height-(rows-1)·gap)/rows`.
- Shrink the tile inside the cell so its aspect ratio stays between **3:4 and 16:9**. The video is `object-cover`, so it crops instead of letterboxing.
- Keep the column count with the **largest tile area**. This one best-fit rule handles portrait phones, landscape tablets and desktops without a table of breakpoints:
  - Two people on a portrait phone stack vertically.
  - Two people on a laptop sit side by side.
  - Twenty people on desktop get a 5×4 grid.
- **Floor:** if the best tile is under `MIN_TILE_WIDTH` (120px), fall back to `floor((width+gap)/(120+gap))` columns of 16:9 tiles with `scroll: true`. This only happens with lots of people on a small phone, and it gets a `ponytail:` note about the limit.
- `count = 0`, or a size of zero, returns 1 column and no NaN.

**New** `web/lib/stage-layout.test.ts` covers: 1 person on desktop fills the stage; 2 people portrait stack; 2 people landscape side by side; 4 people make 2×2; 20 on desktop make 5 columns; 20 at 288px wide scroll; 0 people or a 0 size are safe.

**[video-stage.tsx](web/components/video-stage.tsx)**, grid branch:
- Measure `<main>` with a `ResizeObserver` inside `useLayoutEffect`, reading `getBoundingClientRect` first so there is no first-frame flash.
- Render `grid-template-columns: repeat(cols, tileWidth px)`, centered, with `overflow-y-auto` only when `scroll` is true.
- `Tile` gets an optional `style={{width, height}}`. It keeps `aspect-video` when no size is given, so the presenting filmstrip is unchanged.
- The "Connecting video…" line moves out of the grid into an absolute overlay, so it never counts as a cell.

**[room-shell.tsx](web/components/room-shell.tsx):** change the root from `min-h-dvh` to `h-dvh overflow-hidden`. The stage then has a fixed height to fit into, and the control bar stays pinned at the bottom.

## Task 3 — Control bar at 320px
[control-bar.tsx](web/components/control-bar.tsx):
- Round buttons become `size-11 sm:size-12` (44px minimum kept), with gap `gap-2 sm:gap-3`.
- **Leave** becomes a `PhoneOff` icon with a label that shows from `sm` up, and an `aria-label` always.
- **Hosts get one Leave button instead of two.** It opens the existing AlertDialog with the choices *Keep meeting / Leave / End for all*, like Google Meet. This removes a whole button on phones. Non-hosts leave instantly, as today.
- **ZyloLive is hidden** where `navigator.mediaDevices?.getDisplayMedia` doesn't exist (phones). That check becomes a one-line `canShareScreen()` in [screen-share.ts](web/lib/screen-share.ts), and needs no test. It's safe to call during render because `RoomShell` only ever mounts on the client, after `PreJoin`.
- Result at 320px: 5 controls × 44px + 4 × 8px = 252px, which fits in a single row.

[room-shell.tsx](web/components/room-shell.tsx) header: the host's admission badge is `hidden sm:inline-flex`.

## Task 4 — "Mute for me" (local only)
- [use-livekit-room.ts:135](web/lib/use-livekit-room.ts:135): add `identity: participant.identity` to each audio track entry (identity is the userId, the same key `videoTracks` uses). Update the type at line 70.
- [video-stage.tsx](web/components/video-stage.tsx):
  - `AudioSink` gets a `muted` prop, applied through its ref in an effect (`el.muted = muted`). This deliberately reverses the "never muted" comment for one person, so rewrite the comment to say that.
  - `VideoStage` takes a `mutedForMe: Set<string>`.
  - `Tile` shows a `VolumeX` icon next to `MicOff` when that person is muted for you.
- [room-shell.tsx](web/components/room-shell.tsx): `const [mutedForMe, setMutedForMe] = useState<Set<string>>(new Set())` with a toggle. It lasts only for this session and never touches the server.
- [people-panel.tsx](web/components/people-panel.tsx):
  - Every row except your own gets a ghost icon button (`Volume2` / `VolumeX`, `size-11`) with `aria-pressed`.
  - `aria-label`: "Mute {name} for me" / "Unmute {name} for me". Tooltip: "Only you won't hear them".
  - It shows for everyone, not just hosts, and sits separate from the host's `PersonMenu`.
- Limit: this also silences that person's ZyloLive audio. It gets a `ponytail:` note; split it per source if someone asks.

## Task 5 — ZyloChat toast
**New** `web/lib/chat-toast.ts` + `.test.ts`:
- `shouldToast(msg, selfUserId, chatVisible)` returns false for your own messages and when chat is on screen.
- `toastPreview(text, max = 80)` trims at a word boundary and adds `…`.

In [room-shell.tsx](web/components/room-shell.tsx), an effect keyed on `messages.at(-1)`:
- It's keyed on the last message, not the count, because `use-meeting` slices the list at `MAX_MESSAGES`, so the length stops changing once the cap is reached.
- A ref records the last message at mount, so a remount doesn't toast old history.
- `chatVisible = tab === 'chat' && (sheetOpen || matchMedia('(min-width:1024px)').matches)`, the same pattern `openPanel` uses.
- The toast call:
  ```ts
  toast(name, { id: 'zylochat', description: toastPreview(text), duration: 4000, position: 'top-right', action: { label: 'Open', onClick: () => openPanel('chat') } })
  ```
  - The fixed `id` means a burst of messages replaces one toast instead of stacking.
  - Sonner v2 supports a per-toast `position`; `top-right` keeps it off the room title and the control bar.
  - No new dependency: `sonner` is installed and `<Toaster>` is mounted in [layout.tsx](web/app/layout.tsx).

## Task 6 — ui-ux-pro-max polish pass
- In [globals.css](web/app/globals.css) `@layer base`, add `button:not(:disabled), [role="button"]:not(:disabled) { cursor: pointer; }`. Tailwind v4 removed the pointer cursor from buttons, and the MASTER checklist requires it.
- Fix exactly the Task 1 findings, one component at a time.
- Chat and inputs are already `text-base md:text-sm`, so there's no iOS zoom on focus.
- Run MASTER.md's Pre-Delivery Checklist against every changed component: contrast, focus, touch size, transitions 150–300ms, reduced motion, no emoji icons.
- Deliberately skipped: `viewport-fit=cover` and safe-area insets. Without them, Safari keeps content out of the notch itself. Add them if Zylo ever becomes a PWA or goes fullscreen.

## Verification
1. `cd web && npm test` passes, including the new `stage-layout` and `chat-toast` tests. Mutation check: delete the aspect clamp, the self check and the visibility check one at a time, and watch the named test fail each time.
2. `npm run lint && npm run build` are clean. Server tests are untouched (123 passing).
3. Browser pane: rerun the Task 1 sweep at all 7 widths. The overflow script must return `[]` everywhere, and the room must fit on screen with the control bar visible in one row at 320px. Screenshots go in the PR.
4. **Manual acceptance (user, multiple Chrome profiles, as in Phase 4):**
   - The grid with 1, 2, 3 and 4 people at phone, tablet and desktop widths, including resizing the window live.
   - Mute for me: A mutes B, A stops hearing B, C still hears B, and B's own UI is unchanged.
   - The chat toast appears only when chat is closed, one toast per burst, and "Open" jumps to chat.
   - The host's Leave dialog: both Leave and End for all work.
5. Code review (`/code-review`), then a PR `phase-5-ui` → `main`.

## Critical files
`web/lib/stage-layout.ts` (new), `web/lib/chat-toast.ts` (new), `web/components/video-stage.tsx`, `web/components/room-shell.tsx`, `web/components/control-bar.tsx`, `web/components/people-panel.tsx`, `web/lib/use-livekit-room.ts`, `web/lib/screen-share.ts`, `web/app/globals.css`, spec §Phases.

## Out of scope (named so it isn't lost)
- Phase 6 Translator Convo, planned next.
- Pinning or spotlighting a tile.
- A "+N more" tile instead of scrolling.
- Per-source (mic vs ZyloLive) local mute.
