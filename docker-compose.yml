services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: zylo
      POSTGRES_PASSWORD: zylo
      POSTGRES_DB: zylo
    ports:
      - "5432:5432"
    volumes:
      - zylo-db:/var/lib/postgresql/data
      - ./server/db/create-test-db.sql:/docker-entrypoint-initdb.d/01-create-test-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zylo -d zylo"]
      interval: 2s
      timeout: 3s
      retries: 20

volumes:
  zylo-db:
